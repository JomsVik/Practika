<Window x:Class="ShoeStore.Views.OrderDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Детали заказа" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        FontFamily="Times New Roman"
        Background="White"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Детали заказа" FontSize="24" 
                   FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <Border Grid.Row="1" Background="#F0F0F0" Padding="15" CornerRadius="5" Margin="0,0,0,20">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Номер заказа:" FontWeight="Bold" Margin="0,0,10,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="txtOrderNumber" Margin="0,0,0,5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Дата заказа:" FontWeight="Bold" Margin="0,0,10,5"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="txtOrderDate" Margin="0,0,0,5"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Статус:" FontWeight="Bold" Margin="0,0,10,5"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="txtStatus" Margin="0,0,0,5" FontWeight="Bold"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Код получения:" FontWeight="Bold" Margin="0,0,10,5"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="txtPickupCode" Margin="0,0,0,5"/>
                </Grid>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" Text="Состав заказа:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListView Grid.Row="3" x:Name="listItems" Margin="0,0,0,20">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Товар" Width="250" DisplayMemberBinding="{Binding ProductName}"/>
                    <GridViewColumn Header="Количество" Width="80" DisplayMemberBinding="{Binding Quantity}"/>
                    <GridViewColumn Header="Цена" Width="100" DisplayMemberBinding="{Binding Price, StringFormat={}{0:N0} ₽}"/>
                    <GridViewColumn Header="Сумма" Width="100" DisplayMemberBinding="{Binding Total, StringFormat={}{0:N0} ₽}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Separator Grid.Row="4" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Итого:" FontSize="18" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBlock x:Name="txtTotal" FontSize="18" FontWeight="Bold" Foreground="Green"/>
        </StackPanel>
    </Grid>
</Window>