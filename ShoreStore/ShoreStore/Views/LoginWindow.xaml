<Window x:Class="ShoeStore.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        FontFamily="Times New Roman"
        Background="White"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Вход в систему" FontSize="24" 
                   FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,25"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Логин:" FontSize="14" Margin="0,0,0,5"/>
            <TextBox x:Name="txtLogin" Height="35" Padding="5" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,25">
            <TextBlock Text="Пароль:" FontSize="14" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" Height="35" Padding="5" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
            <Button x:Name="btnLogin" Content="Войти" Width="120" Height="40" 
                    Background="#00FA9A" FontWeight="Bold" FontSize="14"
                    Margin="0,0,15,0" Click="btnLogin_Click"/>
            <Button x:Name="btnGuest" Content="Гость" Width="120" Height="40" 
                    Background="#7FFF00" FontSize="14" Click="btnGuest_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="4" x:Name="txtStatus" Foreground="Red" 
                   HorizontalAlignment="Center" Margin="0,15" FontSize="14"/>

        <TextBlock Grid.Row="4" HorizontalAlignment="Center" Margin="0,0,0,30" FontSize="14">
            <Run Text="Нет аккаунта? "/>
            <Hyperlink Click="RegisterLink_Click">Зарегистрироваться</Hyperlink>
        </TextBlock>
    </Grid>
</Window>